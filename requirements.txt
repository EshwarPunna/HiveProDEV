pandas>=2.0,<3.0
numpy>=1.24
requests>=2.31
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9

# Embeddings for NIST retrieval. Comment out to force the pure-Python
# TF-IDF fallback (scripts/tfidf_fallback.py) if you can't install
# torch in your environment — the system still runs, just with
# weaker semantic matching.
sentence-transformers>=2.6
